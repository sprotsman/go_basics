package main

/*

Maps are a convenient and powerful built-in data structure that associate values
of one type (the key) with values of another type (the element or value).

Aka key-value pairs.

Doesn't allow duplicate keys. Can have duplicate values.

*/

func main() {
	// name map has int 'keys' and string 'values'
	var names map[int]string

	_ = names
}